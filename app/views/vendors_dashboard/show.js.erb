$("#chat-view").html("<%= escape_javascript(render partial: 'layouts/shared/chat_view',:dispute=> @dispute,:messages=>@messages ) %>");
var vendor_dispute_id = "<%= @dispute %>";
$("#unread_dot_"+vendor_dispute_id).remove();
$("#unread_counter_"+vendor_dispute_id).remove();
